<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lirui.Mapper.StudentMapper">
    <insert id="insert">
        insert into student (name,no,gender,phone,id_card,is_college,address,degree,graduation_date,clazz_id,create_time,update_time,violation_count,violation_score)
        values (#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime},#{violationCount},#{violationScore})
    </insert>
    <update id="update">
        update student set name=#{name},no=#{no},gender=#{gender},phone=#{phone},id_card=#{idCard},is_college=#{isCollege},address=#{address},degree=#{degree},graduation_date=#{graduationDate},clazz_id=#{clazzId},update_time=#{updateTime},violation_count=#{violationCount},violation_score=#{violationScore} where id = #{id}
    </update>
    <delete id="delete">
        delete from student where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <select id="list" resultType="com.lirui.Pojo.Student">
        select s.*,c.name clazzName from student s left join clazz c on s.clazz_id = c.id
        <where>
                <if test="name != null and name != ''">
                    s.name like concat('%',#{name},'%')
                </if>
                <if test="degree != null">
                    and s.degree = #{degree}
                </if>
                <if test="clazzId != null">
                    and s.clazz_id = #{clazzId}
                </if>
        </where>
    </select>
    <select id="getStudentDegreeData" resultType="java.util.Map">
        select case
            when degree = 1 then '高中'
            when degree = 2 then '大专'
            when degree = 3 then '本科'
            when degree = 4 then '硕士'
            when degree = 5 then '博士'
            else '其他'
        end name,
        count(*) value from student group by degree
    </select>
    <select id="getStudentCountData" resultType="java.util.Map">
        select c.name clazzName,count(*) num from student s left join clazz c on s.clazz_id = c.id group by c.name order by num
    </select>
</mapper>
